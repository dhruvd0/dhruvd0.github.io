name: dhruvd0
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  auto_route: ^6.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  auto_route_generator: ^6.0.1
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
